package com.infermc.hosecraft.permissions;

import com.infermc.hosecraft.server.Player;

public class PermissionProvider {
    public boolean hasPermission(Player p,String perm) {
        return false;
    }
}
